/**
 * o que é um array ? É uma lista de dados ou um conjunto de dados *
 */

console.log([1, 2, 3, 4]);
console
  .log(["douglas", "pedro", false, true].length)

const douglas = ["douglas", "pedro"].forEach((res, index) => {
    console.log(res, index);
  });
