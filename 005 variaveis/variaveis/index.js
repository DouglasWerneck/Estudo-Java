/*
var  // não é mais utilizado porque ele causa alguns bugs ele e utilizado globalmente
let // é gerado dentro de um escopo
const  // é gerado dentro de um escopo porem podemos armazenar dados apenas 1x
*/

var cachorro = "Kyara"
console.log(cachorro)

let lanche = "bolovo"
console.log(lanche)

const nome = "Douglas"
console.log(nome)